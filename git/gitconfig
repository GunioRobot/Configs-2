[user]
    email = myrseth@gmail.com
    name  = Martin Øinæs Myrseth
[color]
    diff = auto
    status = auto
    branch = auto
[color "branch"]
    current = yellow reverse
    local = yellow 
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    # hist = log --pretty=format:\"%C(yellow)%h %Cgreen%ad %Creset| %s%d [%an]\" --graph --date=short
    hist = log --pretty=format:\"%C(yellow)%h %Cgreen%ad %Creset%s%C(yellow)%d %Creset[%C(cyan)%an%Creset]\" --graph --date=short
